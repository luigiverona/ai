# ai 2.0.1

This correctness release fixes transaction backup naming for Bash and Zsh shell files,
restoring current-transaction interruption recovery across Bash, Zsh, and fish.

The journal now validates the exact canonical backup path derived from the managed shell
path and transaction identifier. No migration or compatibility handling was added; the
fresh-environment scope, collision rules, provenance checks, and safety boundaries remain
unchanged.
